# API Testing

## Dev API Key

Use `dev-kiosk-key` to call the API and test things (sandbox, local, or prod).

Configure via:
- **In-app**: ApiKeySetupScreen — enter `dev-kiosk-key` and select environment
- **Env**: `EXPO_PUBLIC_KIOSK_API_KEY=dev-kiosk-key` or `KIOSK_API_KEY=dev-kiosk-key`
